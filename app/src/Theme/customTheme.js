import { createTheme } from '@mui/material';

const theme = createTheme({      
  typography: {
    button: {
      textTransform: 'none'
    }
  },
  // palette: {
  //   mode: 'dark',
  // },
});

export default theme;